<form method="post" action="{{ path('app_subcategory_delete', {'idsubcategory': subcategory.idsubcategory}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subcategory.idsubcategory) }}">
    <button class="btn">Delete</button>
</form>
